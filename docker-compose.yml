services:
  mock_api:
    build: ./mock_api
    ports:
      - "5000:5000"

  runner:
    build: .
    depends_on:
      - mock_api
    environment:
      - CONFIG_PATH=config.json
    volumes:
      - .:/app
